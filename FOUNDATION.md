# 🏗️ ФУНДАМЕНТ ПРОЕКТА - НЕ МЕНЯТЬ!

## Этот файл содержит правила, которые ОБЯЗАТЕЛЬНЫ для соблюдения при любых изменениях кода.

---

## 📌 ПРАВИЛО 1: Проверка доступности покупок в реальном времени

**Где:** `js/game.js` → `startGameLoops()`

**Что должно быть:**
```javascript
// Обновление UI каждую секунду
setInterval(function() {
  UI.updateCounters();
  UI.updateButtonStates(); // ЭТО ОБЯЗАТЕЛЬНО!
}, 1000);
```

**Зачем:** Кнопки зданий/улучшений должны становиться активными сразу когда хватает ресурсов.

---

## 📌 ПРАВИЛО 2: UI.updateButtonStates() должен существовать

**Где:** `js/ui.js`

**Что должно быть:**
```javascript
updateButtonStates: function() {
  // Обновляет классы кнопок зданий (affordable/disabled)
  // Обновляет классы кнопок улучшений (affordable/disabled/purchased)
}
```

**Зачем:** Визуальное отображение доступности покупок.

---

## 📌 ПРАВИЛО 3: Уведомления не должны накладываться

**Где:** `js/ui.js` → `showAchievementPopup()`

**Что должно быть:**
- Максимум 3 уведомления одновременно
- Очередь для остальных
- Каждое следующее появляется ниже предыдущего

---

## 📌 ПРАВИЛО 4: Единая кнопка меню в шапке

**Где:** `js/ui.js` → `render()` шапка

**Что должно быть:**
- 🌶️ Перчики (магазин)
- 📋 Активности (ВСЁ: игры, челленджи, скины, топ, престиж, настройки, рефералы)

**Кнопки должны быть достаточно большими для мобильных устройств (минимум 36px).**

---

## 📌 ПРАВИЛО 5: Облачное сохранение ВСЕГО

**Где:** `js/supabase.js` → `saveUser()`

**Что должно сохраняться в Supabase:**
- shawarmas, totalShawarmas, lifetimeShawarmas
- perClick, clickCount
- prestigeLevel, prestigeBonus
- buildings (id, owned, cost)
- upgrades (id, purchased)
- achievements (id, unlocked)
- spices (перчики)
- skins (unlocked, current)
- challenges (daily, completed)
- referral (code, referredBy, count)
- settings (theme, music)

---

## 📌 ПРАВИЛО 6: Структура шапки

```
[🌯 Империя Шаурмы ☁️]     [🌶️ XX] [📋]
```

- Название слева
- Перчики и меню справа
- Всё должно помещаться на экране 320px

---

## 📌 ПРАВИЛО 7: Меню Активностей содержит ВСЁ

```
📋 Активности
├── 🎮 Мини-игры
│   └── 🔪 Слайсер
├── 🎯 Челленджи  
├── 🏆 Топ игроков
├── 🎨 Скины
├── ⭐ Престиж (если доступен)
├── 👥 Рефералы
└── ⚙️ Настройки
```

---

## 📌 ПРАВИЛО 8: Размеры кнопок

- Минимум 36x36px для touch-устройств
- Отступы между кнопками минимум 8px

---

## 📌 ПРАВИЛО 9: При любых изменениях проверять:

1. ✅ updateButtonStates() вызывается каждую секунду
2. ✅ Уведомления не накладываются
3. ✅ Кнопки достаточно большие
4. ✅ Всё сохраняется в облако
5. ✅ Шапка помещается на маленьком экране

---

## 🔄 История исправлений:

- 2024-01-26: Исправлена проверка доступности кнопок
- 2024-01-26: Объединены все меню в одну кнопку
- 2024-01-26: Добавлена очередь уведомлений
- 2024-01-26: Расширено облачное сохранение

---

**ВАЖНО:** При каждом изменении кода сверяться с этим файлом!
