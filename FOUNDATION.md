# 🏗️ ФУНДАМЕНТ ПРОЕКТА - НЕ МЕНЯТЬ!

## Этот файл содержит правила, которые ОБЯЗАТЕЛЬНЫ для соблюдения при любых изменениях кода.

---

## 📌 ПРАВИЛО 1: Проверка доступности покупок КАЖДУЮ СЕКУНДУ

**Где:** `js/game.js` → `startGameLoops()`

**Что должно быть:**
```javascript
// ФУНДАМЕНТ: Обновление UI и кнопок КАЖДУЮ СЕКУНДУ
setInterval(function() {
  if (typeof UI !== 'undefined') {
    UI.updateCounters();
    UI.updateButtonStates(); // ОБЯЗАТЕЛЬНО!
  }
}, 1000);
```

**Зачем:** Кнопки зданий/улучшений должны МГНОВЕННО становиться активными когда хватает ресурсов.

---

## 📌 ПРАВИЛО 2: UI.updateButtonStates() ОБЯЗАТЕЛЕН

**Где:** `js/ui.js`

**Функция должна:**
1. Находить ВСЕ кнопки зданий `[data-action="buy-building"]`
2. Проверять стоимость с учётом скидки
3. Добавлять класс `affordable` если хватает денег
4. Добавлять класс `disabled` если не хватает
5. То же самое для улучшений `[data-action="buy-upgrade"]`

---

## 📌 ПРАВИЛО 3: Уведомления с очередью (макс 3)

**Где:** `js/ui.js` → `showAchievementPopup()`

**Что должно быть:**
- `notificationQueue` - массив ожидающих уведомлений
- `activeNotifications` - счётчик активных (макс 3)
- `maxNotifications = 3`
- Новые уведомления добавляются в очередь
- Показываются по мере освобождения места

---

## 📌 ПРАВИЛО 4: Единая кнопка меню (☰)

**Шапка должна содержать ТОЛЬКО:**
```
🌯 Империя Шаурмы ☁️    [🌶️ XX] [☰]
```

**Меню содержит ВСЁ:**
- Мини-игры (Слайсер и др.)
- Челленджи
- Скины
- Топ игроков
- Рефералы
- Престиж (если доступен)
- Настройки (тема, музыка)

---

## 📌 ПРАВИЛО 5: Навигация в подменю

**Все подменю должны иметь:**
- Кнопку `←` (Назад) - возврат в главное меню
- Кнопку `✕` (Закрыть) - закрыть всё

**Пользователь должен иметь возможность "бродить" по меню!**

---

## 📌 ПРАВИЛО 6: Индикатор новых событий

**Кнопка меню должна мигать когда:**
- Готов челлендж к получению награды
- Доступен престиж
- Другие важные события

**Класс:** `.menu-btn.has-notification`

---

## 📌 ПРАВИЛО 7: Анонимизация лидерборда

**До 200 игроков:**
- Все имена = "🎭 Неизвестный игрок"
- Свой аккаунт = "👤 Ты"

**После 200 игроков:**
- Показывать реальные имена

---

## 📌 ПРАВИЛО 8: Размеры кнопок для мобильных

- Минимум **36x36px** для touch-устройств
- Кнопка меню: **40x40px**
- Отступы между кнопками: минимум **8px**

---

## 📌 ПРАВИЛО 9: Облачное сохранение ВСЕГО

**Сохранять в Supabase:**
- Основные данные (shawarmas, buildings, upgrades, achievements)
- Перчики (spices, spices_total)
- Скин (current_skin)
- Тема (theme)
- Музыка (music_enabled)
- Рефералы (referral_code, referral_count)

---

## 🔄 Чеклист при изменениях:

- [ ] updateButtonStates() вызывается каждую секунду?
- [ ] Уведомления используют очередь?
- [ ] Кнопки достаточно большие (36px+)?
- [ ] Подменю имеют кнопку "Назад"?
- [ ] Новые данные сохраняются в облако?
- [ ] Индикатор меню обновляется?

---

## 🔥 НИКОГДА НЕ УДАЛЯТЬ:

1. `updateButtonStates()` функцию
2. Вызов `updateButtonStates()` в `setInterval`
3. Очередь уведомлений
4. Кнопки навигации в подменю

---

**ВАЖНО:** При каждом изменении кода сверяться с этим файлом!
